<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="CoarseFineLocation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarFlip"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
        </activity>

        <activity
            android:name=".Onboarding.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>


        <activity
            android:name=".authentication.Login"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>

        <activity
            android:name=".authentication.forget_passwordActivity"
            android:exported="false"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>

        <activity
            android:name=".authentication.Register2"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>

        <activity android:name=".CarLogoActivity"
            android:exported="true"
            android:theme="@style/Theme.CarFlip.NoActionBarAndStatusBar"/>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>